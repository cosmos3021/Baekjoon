//น้มุ 2743

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main(void) {
	char aaa[101] = { 0 };
	scanf("%s", aaa);
	int len = strlen(aaa);
	printf("%d", len);
	return 0;
}